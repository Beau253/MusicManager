# -----------------------------------------------------------------------------
# MusicManager Environment Variables Template
# -----------------------------------------------------------------------------
# 1. Copy this file to a new file named '.env' in the same directory.
# 2. Fill in your actual secret values in the new '.env' file.
#
# This .env file is used for local development and by docker-compose.
# It MUST NOT be committed to your Git repository.
# -----------------------------------------------------------------------------

# --- User/Group IDs (For Docker Permissions) ---
# Prevents file permission issues on mounted volumes inside the container.
# On Linux/macOS, find these by running the `id` command in your terminal.
# The defaults are common for the first user on many Linux systems.
PUID=1000
PGID=1000

# --- Spotify API Credentials ---
# Required for all Spotify interactions (playlist syncing, track searching, etc.).
# Get these from your Spotify Developer Dashboard: https://developer.spotify.com/dashboard/
SPOTIPY_CLIENT_ID="YOUR_SPOTIFY_CLIENT_ID"
SPOTIPY_CLIENT_SECRET="YOUR_SPOTIFY_CLIENT_SECRET"

# --- Plex API Credentials ---
# Required for the Plex integration features (library scanning, playlist syncing).
# URL should be the full address to your Plex server.
# Find your token: https://support.plex.tv/articles/204059436-finding-an-authentication-token-x-plex-token/
PLEX_URL="http://YOUR_PLEX_IP_OR_HOSTNAME:32400"
PLEX_TOKEN="YOUR_PLEX_TOKEN"

# --- Lidarr API Credentials ---
# Required for the Lidarr integration features (status checking, scan triggers).
# URL should be the full address to your Lidarr instance.
# Find your API Key in Lidarr under Settings > General.
LIDARR_URL="http://YOUR_LIDARR_IP_OR_HOSTNAME:8686"
LIDARR_API_KEY="YOUR_LIDARR_API_KEY"

# --- Discord Webhook (Optional) ---
# Only needed if you want to test the release webhook feature locally.
# In production on GitHub, this is stored as an encrypted repository secret.
# Get this from your Discord server: Server Settings > Integrations > Webhooks
DISCORD_WEBHOOK_URL="YOUR_DISCORD_WEBHOOK_URL"